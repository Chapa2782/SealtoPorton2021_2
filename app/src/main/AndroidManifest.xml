<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sealtosoft.sealtoporton">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SealtoPorton">
        <activity android:name=".gestionPermisos"></activity>
        <activity android:name=".agregarPermiso" />
        <activity android:name=".ComandoInvitado" />
        <activity android:name=".ComandoPropietario"></activity>
        <activity android:name=".InvitadoLogin" />
        <activity android:name=".PropietarioLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>